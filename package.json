{
  "name": "ai-icon-generator",
  "version": "1.8.17",
  "private": true,
  "description": "An AI-powered creative suite to generate vector icons. Use text prompts to create unique icons for your projects.",
  "scripts": {
    "dev": "echo \"Run a static file server for the root directory. E.g., npx serve .\"",
    "build": "echo \"No build step configured.\"",
    "start": "npm run dev"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "@google/genai": "^1.26.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.4.5"
  }
}